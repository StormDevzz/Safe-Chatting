# Safe Chatting

Чат в терминале. Шифрование AES-256-GCM.

```
make        — собрать
make run    — запустить сервер
./chat      — подключиться
```

`@user текст` — написать · `/users` — список · `/exit` — выход
